#include <iostream>#include <fstream>#include <sstream>#include <chrono>#include <iomanip>#include <cmath> #include <random>#include <set>#include <vector>#include <algorithm>#include <hashish.h>void Generatevector(std::vector<std::string> & genvector){	unsigned seed = std::chrono::system_clock::now().time_since_epoch().count();	const std::string VALID_CHARS = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789";	std::mt19937 generator(seed);	std::uniform_int_distribution<std::mt19937::result_type> distribution(0,VALID_CHARS.size() - 1);		std::ostringstream oss;	//std::vector<std::string> genvector;   // std::vector<std::string> Text;	//Text.reserve(100000);	//std::vector<std::string> Text2ndCol;	//Text2ndCol.reserve(100000);//	std::set<std::string> unique;    std::set<std::string> setas;	std::ofstream fr("../randomText/generuotasTekstas.txt");    while(setas.size() != 200000) {        for (std::size_t i = 0; i < 5; ++i)        {            oss << VALID_CHARS[distribution(generator)];        }        std::string first = oss.str();        //std::cout << first << std::endl;        setas.insert(first);        oss.clear();        oss.str("");    }    std::copy(setas.begin(),setas.end(),genvector.begin());    std::vector<std::string>::iterator itr;    int sk=0;    for(int i = 0; i < 200000;i++){        if(i%2==0) {            fr << genvector[i] << " ";        }        else {            fr << genvector[i]<< std::endl;        }    }	/*for(size_t j = 0; j < 100000; j++){		for(size_t j1 = 0; j1 < 2; j1++){			for (std::size_t i = 0; i < 5; ++i)			{			    oss << VALID_CHARS[distribution(generator)];			}		std::string first = oss.str();		if(j1 == 0){			fr << first << " ";			Text.push_back(first);			oss.clear();			oss.str("");			//std::cout << Text[j] << std::endl;		}		if(j1 == 1){			fr << first << std::endl;			Text2ndCol.push_back(first);		}		}		oss.clear();		oss.str("");	}	 */	fr.close();//	std::cout << first << std::endl; 	//genVec.push_back(Text);	//genVec.push_back(Text2ndCol);//}